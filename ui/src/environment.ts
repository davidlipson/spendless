export const host = 'http://localhost:5000';
export const dev = true;

// final to do
// change dev and host to point to heroku and prod
// reset up heroku
// test process button more
// submit to store
